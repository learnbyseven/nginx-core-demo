# Limiting based on connections;
#-------------------------------
upstream backend {
	zone backend 64k;
	server 192.168.1.30:8095;
	}
server {
	limit_conn addr 1;
	status_zone backend;
	location / {
		proxy_pass http://backend;
	}
}
 	
	 
